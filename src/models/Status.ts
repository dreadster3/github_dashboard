export enum EStatus {
    QUEUED = 'queued',
    IN_PROGRESS = 'in_progress',
    COMPLETED = 'completed',
}

export enum EConclusion {
    SUCCESS = 'success',
    FAILURE = 'failure',
    CANCELLED = 'cancelled',
}
